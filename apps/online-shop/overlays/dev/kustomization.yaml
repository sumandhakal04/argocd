apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: onlineshop-dev
resources:
  - ../../base
patchesStrategicMerge:
  - deployment-patch.yaml
  - hpa-patch.yaml
images:
  - name: amitabhdevops/online_shop
    newTag: latest  ## for real apps, when a new commit is created, a docker image with that tag is created and here referred.
